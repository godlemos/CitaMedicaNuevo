buildscript {
    repositories {
        google()  // Repositorio de Google
        mavenCentral()  // Repositorio Maven Central
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'  // Plugin de Android
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.22'  // Plugin de Kotlin
        classpath 'com.google.gms:google-services:4.4.0'  // Plugin de Google Services
    }
} 